{ GameProject(BTTest)
    description string(A test project for the behavior tree components)
    entryPoint Gluon::GameObject(testTree)
    { GluonObject(Assets)
        { BehaviorTree::Asset(test-brain)
            file file(test-brain.xml)
        }
        { Asset(SomeAsset)
            file file(nothing.jpg)
        }
    }
    { GameObject(testTree)
        { BehaviorTree::Tree(tree1)
            behaviorTree BehaviorTree::TreeAsset(Assets.test-brain.tree1)
        }
    }
}
